name: Weekly Guild Board

on:
  schedule:
    # Every Tuesday at 13:00 UTC (9 AM ET) — right before NA reset,
    # so it captures the full previous raid week.
    - cron: "0 13 * * 2"
  # This adds the "Run workflow" button so officers can post/repost
  # the board manually any time (e.g., after updating the roast).
  workflow_dispatch:
    inputs:
      roast:
        description: "Override the roast of the week"
        required: false
      roast_winner:
        description: "Who said the roast"
        required: false
      roast_target:
        description: "Who the roast is aimed at"
        required: false
      difficulty:
        description: "Override raid difficulty (normal, heroic, mythic)"
        required: false
      lookback:
        description: "Override lookback days"
        required: false
      dry_run:
        description: "Dry run only (do not post to Discord)"
        type: boolean
        required: false

jobs:
  post-board:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run tests
        run: pytest

      - name: Build and post the board
        env:
          WCL_CLIENT_ID: ${{ secrets.WCL_CLIENT_ID }}
          WCL_CLIENT_SECRET: ${{ secrets.WCL_CLIENT_SECRET }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          ARGS=""
          if [ -n "${{ github.event.inputs.roast }}" ]; then
            ARGS="$ARGS --roast '${{ github.event.inputs.roast }}'"
          fi
          if [ -n "${{ github.event.inputs.roast_winner }}" ]; then
            ARGS="$ARGS --roast-winner '${{ github.event.inputs.roast_winner }}'"
          fi
          if [ -n "${{ github.event.inputs.roast_target }}" ]; then
            ARGS="$ARGS --roast-target '${{ github.event.inputs.roast_target }}'"
          fi
          if [ -n "${{ github.event.inputs.difficulty }}" ]; then
            ARGS="$ARGS --difficulty ${{ github.event.inputs.difficulty }}"
          fi
          if [ -n "${{ github.event.inputs.lookback }}" ]; then
            ARGS="$ARGS --lookback ${{ github.event.inputs.lookback }}"
          fi
          if [ "${{ github.event.inputs.dry_run }}" == "true" ]; then
            ARGS="$ARGS --dry-run"
          fi
          python leaderboard.py $ARGS

      - name: Commit roster cache
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add roster_cache.json
          if git diff --cached --quiet; then
            echo "No roster cache changes to commit."
          else
            git commit -m "Update roster cache [skip ci]"
            if git pull --rebase origin main; then
              git push
            else
              git checkout --ours roster_cache.json
              git add roster_cache.json
              git rebase --continue
              git push
            fi
          fi
