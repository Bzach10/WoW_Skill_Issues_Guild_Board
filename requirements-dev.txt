# Mirrors the CVE floors in requirements.txt -- see the note there.
requests>=2.32.4
PyYAML>=6.0.2
Pillow>=12.2.0
pytest>=7.0
ruff>=0.4.0
mypy>=1.8.0

# Security scanners. scripts/security_check.py degrades to SKIP without
# these, so a plain checkout still runs; CI installs them explicitly.
pip-audit>=2.10.1
bandit>=1.7.7
